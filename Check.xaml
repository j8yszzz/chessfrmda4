<Window x:Class="Chess.Check"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Check!" 
        Height="150" 
        Width="300"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#1a1a2e"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True">

    <Border Background="#16213e" 
            CornerRadius="12" 
            BorderThickness="3"
            BorderBrush="#e94560">

        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">

            <TextBlock Text="⚠ CHECK!" 
                      Foreground="#e94560" 
                      FontSize="36"
                      FontWeight="Bold"
                      HorizontalAlignment="Center"
                      Margin="0,20,0,10"/>

            <TextBlock Text="Your king is under attack!" 
                      Foreground="White" 
                      FontSize="16"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,20"/>

            <Button Content="Dismiss" 
                   Background="#e94560"
                   Foreground="White"
                   FontSize="14"
                   FontWeight="Bold"
                   Padding="30,8"
                   BorderThickness="0"
                   Cursor="Hand"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"
                   Click="Dismiss_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                BorderThickness="2"
                                BorderBrush="#c73549">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
                <Button.Style>
                    <Style TargetType="Button">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#d94560"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Border>
</Window>